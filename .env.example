# Postgres (used by n8n)
POSTGRES_USER=n8n
POSTGRES_PASSWORD=change_me
POSTGRES_DB_NAME=n8n

# n8n host used to construct webhook URLs (change if using a domain)
N8N_HOST=localhost
# Optional: public webhook tunnel URL (ngrok, cloudflare tunnel, etc.)
WEBHOOK_TUNNEL_URL=

# Service credentials (fill these with your real secrets)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
GOOGLE_PALM_API_KEY=your_google_palm_api_key_here
